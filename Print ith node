void printIthNode(Node *head, int i)
{
    Node*temp =head;
    int count =0;
    while( temp!=NULL){
        if(count==i)
            cout<<temp->data<<endl;
      
        
        temp= temp->next;
        count++;
        
    }
     

    
     
}
